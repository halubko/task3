import React from "react"
import { Box, Container } from "@mui/material"

const SignUpPage = () => {
   return (
      <Container component="main" sx={{ height: "100vh", display: "flex", alignItems: "center" }}>
         <Box sx={{ width: "100%" }}>SI</Box>
      </Container>
   )
}

export default SignUpPage
